{
  "name": "palewire-mastodon-well-known-proxy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "types:check": "wrangler types --check",
    "config:check": "wrangler deploy --dry-run --env=\"\" && wrangler deploy --dry-run --env startup-canary && wrangler deploy --dry-run --env same-zone-canary",
    "validate": "npm run types:check && npm run typecheck && npm run config:check"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.125.0"
  }
}
